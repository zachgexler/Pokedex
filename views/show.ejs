<!DOCTYPE html>
<html>
  <link rel="stylesheet" href="/style.css">
  <head>
    <meta charset="utf-8" />
    <title>Pokedex | <%= pokemonId.name %></title>
  </head>
  <body>
    <h1><%=pokemonId.name%></h1>
    <img src="<%=pokemonId.img%>">
    <h4>Type</h4>
    <ul>
        <% for (i = 0; i < pokemonId.type.length; i++) { %>
            <li>
                <%=pokemonId.type[i]%>
            </li>
       <% } %>
    </ul>
    <h4>Stats</h4>
    <% for (const stats in pokemonId.stats) {%>
    <p>
        <%=stats%>: <%=pokemonId.stats[stats]%>
    </p>
    <% } %>

  </body>
</html>